name: Deploy

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Say hello
      shell: bash
      run: echo "Hello, world!"

    - name: What time is it?
      shell: bash
      run: date
